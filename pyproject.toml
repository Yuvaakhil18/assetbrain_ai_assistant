[project]
name = "unified-asset-reasoning"
version = "0.1.0"
description = "Backend for Unified Asset Reasoning Layer"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "pydantic>=2.0.0"
]

[tool.vercel]
entrypoint = "api.index:app"
